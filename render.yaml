services:
  - type: web
    name: alibaba-survey
    env: node
    buildCommand: echo "Building..."
    startCommand: node server/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: ADMIN_PASSWORD
        generateValue: false
        sync: false
